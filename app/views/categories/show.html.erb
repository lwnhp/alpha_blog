<h1 class="center">Artikelen met categorie "<%= "#{@category.name}" %>": </h1>

<div class="row" align="center">
  <% if logged_in? && current_user.admin? %>
    <span class=badge>
      <%= link_to "wijzig categorie naam", edit_category_path(@category) %>
    </span>
  <% end %>
</div>

<div align="center">
  <%= will_paginate @category_articles %>
</div>

<%= render 'articles/article', obj: @category_articles %>

<div align="center">
  <%= will_paginate @category_articles %>
</div>